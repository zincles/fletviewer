name: fletviewer_frontend
description: Experimental Flutter GUI with an embedded fvcore Runtime.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_rust_bridge: 2.12.0
  flutter_staggered_grid_view: ^0.7.0
  fvcore:
    path: rust_builder
  path_provider: ^2.1.5
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
